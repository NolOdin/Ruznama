# Инструкция по тестированию системы виджетов

## Быстрый старт

### 1. Запустите приложение

```bash
npm start
```

или

```bash
ng serve
```

### 2. Откройте браузер

Перейдите на `http://localhost:4200`

### 3. Тестирование виджетов

#### Шаг 1: Переключение между таблицей и виджетами
- В правом верхнем углу появилась панель управления
- Нажмите кнопку **"Показать виджеты"**
- Вы увидите контейнер с виджетами

#### Шаг 2: Проверка автоматической инициализации
- При первом запуске автоматически создаются 2 тестовых виджета:
  - **Время молитв** - виджет с данными о времени молитв
  - **Тестовый виджет** - кастомный виджет-заглушка

#### Шаг 3: Добавление виджетов
В панели управления доступны кнопки:
- **➕ Добавить виджет молитв** - создает новый виджет времени молитв
- **➕ Добавить кастомный виджет** - создает кастомный виджет
- **🗑️ Удалить все** - удаляет все виджеты
- **Виджетов: X** - показывает количество виджетов

#### Шаг 4: Проверка сохранения
- Добавьте несколько виджетов
- Обновите страницу (F5)
- Виджеты должны сохраниться и восстановиться из localStorage

#### Шаг 5: Проверка виджета времени молитв
- Убедитесь, что виджет "Время молитв" отображает:
  - Текущую дату (день, месяц, год)
  - Время всех молитв (Фаджр, Восход, Зухр, Аср, Магриб, Иша)

## Что проверить

### ✅ Основной функционал
- [ ] Виджеты создаются и отображаются
- [ ] Виджеты сохраняются в localStorage
- [ ] Виджеты восстанавливаются при перезагрузке страницы
- [ ] Виджеты можно удалять
- [ ] Данные передаются в виджеты времени молитв

### ✅ Интерфейс
- [ ] Панель управления видна и функциональна
- [ ] Виджеты отображаются в сетке
- [ ] Виджеты имеют заголовки
- [ ] Стили применяются корректно

### ✅ Реактивность
- [ ] При добавлении виджета он сразу появляется
- [ ] При удалении виджета он сразу исчезает
- [ ] Счетчик виджетов обновляется

## Тестирование через консоль браузера

Вы также можете тестировать систему виджетов через консоль разработчика:

### Открыть консоль
Нажмите `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)

### Примеры команд

```javascript
// Получить сервис виджетов
const injector = ng.probe(document.body).injector;
const widgetService = injector.get(ng.probe(document.body).componentInstance.constructor.ctor[1]);

// Получить все виджеты
console.log(widgetService.getWidgets());

// Добавить виджет программно
widgetService.addWidget({
  id: 'test-' + Date.now(),
  title: 'Тестовый виджет',
  type: 'custom',
  enabled: true,
  size: { width: 300, height: 200 },
  config: { test: true }
});
```

## Отладка

### Проверка localStorage
```javascript
// В консоли браузера
console.log(localStorage.getItem('ruznama-widgets'));
```

### Очистка данных
```javascript
// Удалить все виджеты из localStorage
localStorage.removeItem('ruznama-widgets');
location.reload();
```

## Следующие шаги

После базового тестирования можно:
1. Создать собственные виджеты
2. Настроить стили виджетов
3. Добавить drag-and-drop для перестановки
4. Добавить настройки виджетов (resize, position)

## Удаление тестового кода

После тестирования вы можете:
- Удалить метод `initializeTestWidgets()` из `app.component.ts`
- Убрать панель управления из `app.component.html`
- Или оставить её для будущего использования

